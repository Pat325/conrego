{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block body %}

<div class="container">
    <div class="section">
        
        <div class="row">
            <div class="col-6 mt-5 mb-5">           
                {{ form(form) }}            
            </div>
            <div class="col-6">
                <canvas id="myChart" width="100%"></canvas>
            </div>
        </div>
        
        <div class="row text-center mt-2">   
            <a href="{{ path('pdf') }}"><button type="button" class="btn btn-secondary">Pobierz PDF</button></a>
        </div>

        <div class="row mt-3">   
            <div class="col-12 text-center" id="messages"></div>
        </div>

    </div>
</div>

{% endblock %}
